const initialState = {
    selectedUser: '',
    selectedCV: '',
}

const CVreducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default CVreducer
